import 'package:flutter/widgets.dart';

class StandaloneHome extends StatelessWidget {
  const StandaloneHome({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
