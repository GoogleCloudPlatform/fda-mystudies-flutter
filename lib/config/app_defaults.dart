class AppDefaults {
  // order defines questionnaire-order different than the one received from
  // backend. Since there isn't currently a way to reorder questionnaires in
  // study-builder this is a temporary way to do it.
  static final order = [];
}
